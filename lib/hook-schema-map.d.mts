// Auto-generated by scripts/build-hook-schema.mjs — do not edit manually.

import type { HookSchemaMap } from "./hook-schema-builder.mjs"

/** Typed hook schema map with per-hook, per-handler-type field definitions. */
export declare const HOOK_SCHEMA_MAP: HookSchemaMap

/** Precise typed interface for the hook schema map. */
export interface HookSchemaMapTyped {
  ConfigChange: {
    command: {
      settings: {
        /** Config source to filter on. */
        matcher?:
          | "user_settings"
          | "project_settings"
          | "local_settings"
          | "policy_settings"
          | "skills"
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Absolute path to the changed configuration file. */
        file_path: string
        /** Which configuration source changed. */
        source:
          | "user_settings"
          | "project_settings"
          | "local_settings"
          | "policy_settings"
          | "skills"
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to block the config change. */
        decision?: "block"
      }
    }
    http: {
      settings: {
        /** Config source to filter on. */
        matcher?:
          | "user_settings"
          | "project_settings"
          | "local_settings"
          | "policy_settings"
          | "skills"
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Absolute path to the changed configuration file. */
        file_path: string
        /** Which configuration source changed. */
        source:
          | "user_settings"
          | "project_settings"
          | "local_settings"
          | "policy_settings"
          | "skills"
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to block the config change. */
        decision?: "block"
      }
    }
  }
  CwdChanged: {
    command: {
      settings: {
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Previous working directory path. */
        old_cwd: string
        /** New working directory path. */
        new_cwd: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Previous working directory path. */
        old_cwd: string
        /** New working directory path. */
        new_cwd: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  Elicitation: {
    command: {
      settings: {
        /** MCP server name to filter on. */
        matcher?: string
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the MCP server requesting user input. */
        mcp_server_name: string
        /** The prompt message shown to the user. */
        message: string
        /** Elicitation mode. */
        mode: string
        /** URL associated with the elicitation. */
        url: string
        /** Unique identifier for this elicitation request. */
        elicitation_id: string
        /** JSON schema describing the expected response format. */
        requested_schema: Record<string, unknown>
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Elicitation-specific output fields. */
        hookSpecificOutput?: {
          /** Controls the elicitation response. */
          action?: "accept" | "decline" | "cancel"
          /** Response content to send back. */
          content?: Record<string, unknown>
        }
      }
    }
    http: {
      settings: {
        /** MCP server name to filter on. */
        matcher?: string
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the MCP server requesting user input. */
        mcp_server_name: string
        /** The prompt message shown to the user. */
        message: string
        /** Elicitation mode. */
        mode: string
        /** URL associated with the elicitation. */
        url: string
        /** Unique identifier for this elicitation request. */
        elicitation_id: string
        /** JSON schema describing the expected response format. */
        requested_schema: Record<string, unknown>
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Elicitation-specific output fields. */
        hookSpecificOutput?: {
          /** Controls the elicitation response. */
          action?: "accept" | "decline" | "cancel"
          /** Response content to send back. */
          content?: Record<string, unknown>
        }
      }
    }
  }
  ElicitationResult: {
    command: {
      settings: {
        /** MCP server name to filter on. */
        matcher?: string
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the MCP server. */
        mcp_server_name: string
        /** The user's response to the elicitation. */
        user_response: Record<string, unknown>
        /** The original prompt message. */
        message: string
        /** Unique identifier matching the original Elicitation. */
        elicitation_id: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** ElicitationResult-specific output fields. */
        hookSpecificOutput?: {
          /** Override the user response action. */
          action?: "accept" | "decline" | "cancel"
          /** Replacement response content. */
          content?: Record<string, unknown>
        }
      }
    }
    http: {
      settings: {
        /** MCP server name to filter on. */
        matcher?: string
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the MCP server. */
        mcp_server_name: string
        /** The user's response to the elicitation. */
        user_response: Record<string, unknown>
        /** The original prompt message. */
        message: string
        /** Unique identifier matching the original Elicitation. */
        elicitation_id: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** ElicitationResult-specific output fields. */
        hookSpecificOutput?: {
          /** Override the user response action. */
          action?: "accept" | "decline" | "cancel"
          /** Replacement response content. */
          content?: Record<string, unknown>
        }
      }
    }
  }
  FileChanged: {
    command: {
      settings: {
        /** REQUIRED pipe-separated basenames to watch. Example: ".envrc|.env". */
        matcher: string
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Full path to the changed file. */
        file_path: string
        /** Type of file system event (e.g. "change"). */
        event: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Array of absolute paths. Replaces the current dynamic watch list. */
        watchPaths?: string[]
      }
    }
    http: {
      settings: {
        /** REQUIRED pipe-separated basenames to watch. Example: ".envrc|.env". */
        matcher: string
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Full path to the changed file. */
        file_path: string
        /** Type of file system event (e.g. "change"). */
        event: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Array of absolute paths. Replaces the current dynamic watch list. */
        watchPaths?: string[]
      }
    }
  }
  InstructionsLoaded: {
    command: {
      settings: {
        /** Load reason to filter on. */
        matcher?:
          | "session_start"
          | "nested_traversal"
          | "path_glob_match"
          | "include"
          | "compact"
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Path to the loaded instruction file. */
        file_path: string
        /** Type of memory/instruction being loaded. */
        memory_type: string
        /** Why the instruction file was loaded. */
        load_reason:
          | "session_start"
          | "nested_traversal"
          | "path_glob_match"
          | "include"
          | "compact"
        /** Glob patterns that triggered the load (only for path_glob_match). */
        globs?: Record<string, unknown>
        /** Path to the file that triggered loading. */
        trigger_file_path?: string
        /** Path to the parent instruction file (for include reason). */
        parent_file_path?: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** Load reason to filter on. */
        matcher?:
          | "session_start"
          | "nested_traversal"
          | "path_glob_match"
          | "include"
          | "compact"
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Path to the loaded instruction file. */
        file_path: string
        /** Type of memory/instruction being loaded. */
        memory_type: string
        /** Why the instruction file was loaded. */
        load_reason:
          | "session_start"
          | "nested_traversal"
          | "path_glob_match"
          | "include"
          | "compact"
        /** Glob patterns that triggered the load (only for path_glob_match). */
        globs?: Record<string, unknown>
        /** Path to the file that triggered loading. */
        trigger_file_path?: string
        /** Path to the parent instruction file (for include reason). */
        parent_file_path?: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  Notification: {
    command: {
      settings: {
        /** Notification type to filter on. */
        matcher?:
          | "permission_prompt"
          | "idle_prompt"
          | "auth_success"
          | "elicitation_dialog"
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Type of notification being sent. */
        notification_type:
          | "permission_prompt"
          | "idle_prompt"
          | "auth_success"
          | "elicitation_dialog"
        /** Notification message body. */
        message: string
        /** Notification title. */
        title: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** Notification type to filter on. */
        matcher?:
          | "permission_prompt"
          | "idle_prompt"
          | "auth_success"
          | "elicitation_dialog"
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Type of notification being sent. */
        notification_type:
          | "permission_prompt"
          | "idle_prompt"
          | "auth_success"
          | "elicitation_dialog"
        /** Notification message body. */
        message: string
        /** Notification title. */
        title: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  PermissionRequest: {
    command: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Suggestions for the permission prompt. Shape varies. */
        permission_suggestions?: Record<string, unknown>
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** PermissionRequest-specific output fields. */
        hookSpecificOutput?: {
          /** Permission decision object. */
          decision?: {
            /** Permission decision. "allow" grants, "deny" blocks. */
            behavior?: "allow" | "deny"
          }
        }
      }
    }
    prompt: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** Prompt text sent to a Claude model. Use $ARGUMENTS for dynamic input. */
        prompt: string
        /** Model to use for the prompt. */
        model?: "opus" | "sonnet" | "haiku" | "opus[4m]" | "sonnet[4m]"
        /** Seconds before canceling. Default: 30 for prompt hooks. */
        timeout?: number
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Suggestions for the permission prompt. Shape varies. */
        permission_suggestions?: Record<string, unknown>
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** PermissionRequest-specific output fields. */
        hookSpecificOutput?: {
          /** Permission decision object. */
          decision?: {
            /** Permission decision. "allow" grants, "deny" blocks. */
            behavior?: "allow" | "deny"
          }
        }
      }
    }
    agent: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** Prompt text sent to the Claude model. $ARGUMENTS is replaced with hook context. */
        prompt: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Suggestions for the permission prompt. Shape varies. */
        permission_suggestions?: Record<string, unknown>
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** PermissionRequest-specific output fields. */
        hookSpecificOutput?: {
          /** Permission decision object. */
          decision?: {
            /** Permission decision. "allow" grants, "deny" blocks. */
            behavior?: "allow" | "deny"
          }
        }
      }
    }
    http: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Suggestions for the permission prompt. Shape varies. */
        permission_suggestions?: Record<string, unknown>
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** PermissionRequest-specific output fields. */
        hookSpecificOutput?: {
          /** Permission decision object. */
          decision?: {
            /** Permission decision. "allow" grants, "deny" blocks. */
            behavior?: "allow" | "deny"
          }
        }
      }
    }
  }
  PostCompact: {
    command: {
      settings: {
        /** Compact trigger to filter on. */
        matcher?: "manual" | "auto"
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** What triggered the compaction. */
        trigger: "manual" | "auto"
        /** The summary text produced by the compaction operation. */
        compact_summary: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** Compact trigger to filter on. */
        matcher?: "manual" | "auto"
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** What triggered the compaction. */
        trigger: "manual" | "auto"
        /** The summary text produced by the compaction operation. */
        compact_summary: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  PostToolUse: {
    command: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Unique identifier for this tool call invocation. */
        tool_use_id: string
        /** The tool's return value. Shape varies by tool. */
        tool_response: Record<string, unknown>
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to stop Claude from continuing after this tool call. */
        decision?: "block"
      }
    }
    prompt: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** Prompt text sent to a Claude model. Use $ARGUMENTS for dynamic input. */
        prompt: string
        /** Model to use for the prompt. */
        model?: "opus" | "sonnet" | "haiku" | "opus[4m]" | "sonnet[4m]"
        /** Seconds before canceling. Default: 30 for prompt hooks. */
        timeout?: number
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Unique identifier for this tool call invocation. */
        tool_use_id: string
        /** The tool's return value. Shape varies by tool. */
        tool_response: Record<string, unknown>
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to stop Claude from continuing after this tool call. */
        decision?: "block"
      }
    }
    agent: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** Prompt text sent to the Claude model. $ARGUMENTS is replaced with hook context. */
        prompt: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Unique identifier for this tool call invocation. */
        tool_use_id: string
        /** The tool's return value. Shape varies by tool. */
        tool_response: Record<string, unknown>
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to stop Claude from continuing after this tool call. */
        decision?: "block"
      }
    }
    http: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Unique identifier for this tool call invocation. */
        tool_use_id: string
        /** The tool's return value. Shape varies by tool. */
        tool_response: Record<string, unknown>
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to stop Claude from continuing after this tool call. */
        decision?: "block"
      }
    }
  }
  PostToolUseFailure: {
    command: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Unique identifier for this tool call invocation. */
        tool_use_id: string
        /** Error message from the failed tool call. */
        error: string
        /** True if the failure was caused by a user interrupt rather than a tool error. */
        is_interrupt: boolean
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    prompt: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** Prompt text sent to a Claude model. Use $ARGUMENTS for dynamic input. */
        prompt: string
        /** Model to use for the prompt. */
        model?: "opus" | "sonnet" | "haiku" | "opus[4m]" | "sonnet[4m]"
        /** Seconds before canceling. Default: 30 for prompt hooks. */
        timeout?: number
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Unique identifier for this tool call invocation. */
        tool_use_id: string
        /** Error message from the failed tool call. */
        error: string
        /** True if the failure was caused by a user interrupt rather than a tool error. */
        is_interrupt: boolean
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    agent: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** Prompt text sent to the Claude model. $ARGUMENTS is replaced with hook context. */
        prompt: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Unique identifier for this tool call invocation. */
        tool_use_id: string
        /** Error message from the failed tool call. */
        error: string
        /** True if the failure was caused by a user interrupt rather than a tool error. */
        is_interrupt: boolean
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Unique identifier for this tool call invocation. */
        tool_use_id: string
        /** Error message from the failed tool call. */
        error: string
        /** True if the failure was caused by a user interrupt rather than a tool error. */
        is_interrupt: boolean
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  PreCompact: {
    command: {
      settings: {
        /** Compact trigger to filter on. */
        matcher?: "manual" | "auto"
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
        /** When true, fires only once per session. Subsequent events are skipped. */
        once?: boolean
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** What triggered the compaction. */
        trigger: "manual" | "auto"
        /** Custom compaction instructions provided by the user, if any. */
        custom_instructions?: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** Compact trigger to filter on. */
        matcher?: "manual" | "auto"
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
        /** When true, fires only once per session. Subsequent events are skipped. */
        once?: boolean
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** What triggered the compaction. */
        trigger: "manual" | "auto"
        /** Custom compaction instructions provided by the user, if any. */
        custom_instructions?: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  PreToolUse: {
    command: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Unique identifier for this specific tool call invocation. */
        tool_use_id: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** PreToolUse-specific output fields. */
        hookSpecificOutput?: {
          /** Controls whether the tool call proceeds. */
          permissionDecision?: "allow" | "deny" | "ask"
          /** Explanation shown to the model when permissionDecision is "deny". */
          permissionDecisionReason?: string
          /** When true, auto-approves all future uses of this tool for the rest of the session. */
          autoAllow?: boolean
          /** Modified tool input that replaces the original. For AskUserQuestion, can include {question, answer} to auto-respond. */
          updatedInput?: Record<string, unknown>
        }
      }
    }
    prompt: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** Prompt text sent to a Claude model. Use $ARGUMENTS for dynamic input. */
        prompt: string
        /** Model to use for the prompt. */
        model?: "opus" | "sonnet" | "haiku" | "opus[4m]" | "sonnet[4m]"
        /** Seconds before canceling. Default: 30 for prompt hooks. */
        timeout?: number
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Unique identifier for this specific tool call invocation. */
        tool_use_id: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** PreToolUse-specific output fields. */
        hookSpecificOutput?: {
          /** Controls whether the tool call proceeds. */
          permissionDecision?: "allow" | "deny" | "ask"
          /** Explanation shown to the model when permissionDecision is "deny". */
          permissionDecisionReason?: string
          /** When true, auto-approves all future uses of this tool for the rest of the session. */
          autoAllow?: boolean
          /** Modified tool input that replaces the original. For AskUserQuestion, can include {question, answer} to auto-respond. */
          updatedInput?: Record<string, unknown>
        }
      }
    }
    agent: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** Prompt text sent to the Claude model. $ARGUMENTS is replaced with hook context. */
        prompt: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Unique identifier for this specific tool call invocation. */
        tool_use_id: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** PreToolUse-specific output fields. */
        hookSpecificOutput?: {
          /** Controls whether the tool call proceeds. */
          permissionDecision?: "allow" | "deny" | "ask"
          /** Explanation shown to the model when permissionDecision is "deny". */
          permissionDecisionReason?: string
          /** When true, auto-approves all future uses of this tool for the rest of the session. */
          autoAllow?: boolean
          /** Modified tool input that replaces the original. For AskUserQuestion, can include {question, answer} to auto-respond. */
          updatedInput?: Record<string, unknown>
        }
      }
    }
    http: {
      settings: {
        /** Regex pattern matching tool names (e.g. "Bash", "Edit|Write"). */
        matcher?: string
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the tool being called (e.g. "Bash", "Edit", "mcp__server__tool"). */
        tool_name: string
        /** Arguments passed to the tool as key-value pairs. Shape varies by tool. */
        tool_input: Record<string, unknown>
        /** Unique identifier for this specific tool call invocation. */
        tool_use_id: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** PreToolUse-specific output fields. */
        hookSpecificOutput?: {
          /** Controls whether the tool call proceeds. */
          permissionDecision?: "allow" | "deny" | "ask"
          /** Explanation shown to the model when permissionDecision is "deny". */
          permissionDecisionReason?: string
          /** When true, auto-approves all future uses of this tool for the rest of the session. */
          autoAllow?: boolean
          /** Modified tool input that replaces the original. For AskUserQuestion, can include {question, answer} to auto-respond. */
          updatedInput?: Record<string, unknown>
        }
      }
    }
  }
  SessionEnd: {
    command: {
      settings: {
        /** Session end reason to filter on. */
        matcher?:
          | "clear"
          | "resume"
          | "logout"
          | "prompt_input_exit"
          | "bypass_permissions_disabled"
          | "other"
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
        /** When true, fires only once per session. Subsequent events are skipped. */
        once?: boolean
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Why the session ended. */
        reason:
          | "clear"
          | "resume"
          | "logout"
          | "prompt_input_exit"
          | "bypass_permissions_disabled"
          | "other"
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** Session end reason to filter on. */
        matcher?:
          | "clear"
          | "resume"
          | "logout"
          | "prompt_input_exit"
          | "bypass_permissions_disabled"
          | "other"
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
        /** When true, fires only once per session. Subsequent events are skipped. */
        once?: boolean
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Why the session ended. */
        reason:
          | "clear"
          | "resume"
          | "logout"
          | "prompt_input_exit"
          | "bypass_permissions_disabled"
          | "other"
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  SessionStart: {
    command: {
      settings: {
        /** Session start source to filter on. */
        matcher?: "startup" | "resume" | "clear" | "compact"
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
        /** When true, fires only once per session. Subsequent events are skipped. */
        once?: boolean
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** The Claude model being used (e.g. "claude-opus-4-6"). */
        model: string
        /** How the session started. */
        source: "startup" | "resume" | "clear" | "compact"
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  Setup: {
    command: {
      settings: {
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  Stop: {
    command: {
      settings: {
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Claude's final response text for this turn. */
        last_assistant_message: string
        /** True if a stop hook is already running. Check this to avoid recursion. */
        stop_hook_active: boolean
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to re-engage Claude for another turn. */
        decision?: "block"
      }
    }
    prompt: {
      settings: {
        /** Prompt text sent to a Claude model. Use $ARGUMENTS for dynamic input. */
        prompt: string
        /** Model to use for the prompt. */
        model?: "opus" | "sonnet" | "haiku" | "opus[4m]" | "sonnet[4m]"
        /** Seconds before canceling. Default: 30 for prompt hooks. */
        timeout?: number
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Claude's final response text for this turn. */
        last_assistant_message: string
        /** True if a stop hook is already running. Check this to avoid recursion. */
        stop_hook_active: boolean
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to re-engage Claude for another turn. */
        decision?: "block"
      }
    }
    agent: {
      settings: {
        /** Prompt text sent to the Claude model. $ARGUMENTS is replaced with hook context. */
        prompt: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Claude's final response text for this turn. */
        last_assistant_message: string
        /** True if a stop hook is already running. Check this to avoid recursion. */
        stop_hook_active: boolean
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to re-engage Claude for another turn. */
        decision?: "block"
      }
    }
    http: {
      settings: {
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Claude's final response text for this turn. */
        last_assistant_message: string
        /** True if a stop hook is already running. Check this to avoid recursion. */
        stop_hook_active: boolean
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to re-engage Claude for another turn. */
        decision?: "block"
      }
    }
  }
  StopFailure: {
    command: {
      settings: {
        /** Error type to filter on. */
        matcher?:
          | "rate_limit"
          | "authentication_failed"
          | "billing_error"
          | "invalid_request"
          | "server_error"
          | "max_output_tokens"
          | "unknown"
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** The API error type that caused the failure. */
        error:
          | "rate_limit"
          | "authentication_failed"
          | "billing_error"
          | "invalid_request"
          | "server_error"
          | "max_output_tokens"
          | "unknown"
        /** Additional error details. */
        error_details: Record<string, unknown>
        /** Claude's last response text before the error. */
        last_assistant_message: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** Error type to filter on. */
        matcher?:
          | "rate_limit"
          | "authentication_failed"
          | "billing_error"
          | "invalid_request"
          | "server_error"
          | "max_output_tokens"
          | "unknown"
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** The API error type that caused the failure. */
        error:
          | "rate_limit"
          | "authentication_failed"
          | "billing_error"
          | "invalid_request"
          | "server_error"
          | "max_output_tokens"
          | "unknown"
        /** Additional error details. */
        error_details: Record<string, unknown>
        /** Claude's last response text before the error. */
        last_assistant_message: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  SubagentStart: {
    command: {
      settings: {
        /** Agent type to filter on. Built-in: "Bash", "Explore", "Plan", or a custom agent name. */
        matcher?: string
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Unique identifier for this subagent instance. */
        agent_id: string
        /** Type of agent starting. Built-in: "Bash", "Explore", "Plan", or a custom agent name. */
        agent_type: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** Agent type to filter on. Built-in: "Bash", "Explore", "Plan", or a custom agent name. */
        matcher?: string
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Unique identifier for this subagent instance. */
        agent_id: string
        /** Type of agent starting. Built-in: "Bash", "Explore", "Plan", or a custom agent name. */
        agent_type: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  SubagentStop: {
    command: {
      settings: {
        /** Agent type to filter on. Built-in: "Bash", "Explore", "Plan", or a custom agent name. */
        matcher?: string
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Unique identifier for the completed subagent. */
        agent_id: string
        /** Type of the completed agent. */
        agent_type: string
        /** The subagent's final response text. */
        last_assistant_message: string
        /** Path to the subagent's full transcript JSON file. */
        agent_transcript_path: string
        /** True if a stop hook is already running. */
        stop_hook_active: boolean
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to stop execution after subagent completion. */
        decision?: "block"
      }
    }
    prompt: {
      settings: {
        /** Agent type to filter on. Built-in: "Bash", "Explore", "Plan", or a custom agent name. */
        matcher?: string
        /** Prompt text sent to a Claude model. Use $ARGUMENTS for dynamic input. */
        prompt: string
        /** Model to use for the prompt. */
        model?: "opus" | "sonnet" | "haiku" | "opus[4m]" | "sonnet[4m]"
        /** Seconds before canceling. Default: 30 for prompt hooks. */
        timeout?: number
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Unique identifier for the completed subagent. */
        agent_id: string
        /** Type of the completed agent. */
        agent_type: string
        /** The subagent's final response text. */
        last_assistant_message: string
        /** Path to the subagent's full transcript JSON file. */
        agent_transcript_path: string
        /** True if a stop hook is already running. */
        stop_hook_active: boolean
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to stop execution after subagent completion. */
        decision?: "block"
      }
    }
    agent: {
      settings: {
        /** Agent type to filter on. Built-in: "Bash", "Explore", "Plan", or a custom agent name. */
        matcher?: string
        /** Prompt text sent to the Claude model. $ARGUMENTS is replaced with hook context. */
        prompt: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Unique identifier for the completed subagent. */
        agent_id: string
        /** Type of the completed agent. */
        agent_type: string
        /** The subagent's final response text. */
        last_assistant_message: string
        /** Path to the subagent's full transcript JSON file. */
        agent_transcript_path: string
        /** True if a stop hook is already running. */
        stop_hook_active: boolean
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to stop execution after subagent completion. */
        decision?: "block"
      }
    }
    http: {
      settings: {
        /** Agent type to filter on. Built-in: "Bash", "Explore", "Plan", or a custom agent name. */
        matcher?: string
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Unique identifier for the completed subagent. */
        agent_id: string
        /** Type of the completed agent. */
        agent_type: string
        /** The subagent's final response text. */
        last_assistant_message: string
        /** Path to the subagent's full transcript JSON file. */
        agent_transcript_path: string
        /** True if a stop hook is already running. */
        stop_hook_active: boolean
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Set to "block" to stop execution after subagent completion. */
        decision?: "block"
      }
    }
  }
  TaskCompleted: {
    command: {
      settings: {
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Unique identifier for the completed task. */
        task_id: string
        /** Subject line of the completed task. */
        task_subject: string
        /** Full description of the completed task. */
        task_description: string
        /** Name of the teammate that completed the task. */
        teammate_name: string
        /** Name of the agent team. */
        team_name: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    prompt: {
      settings: {
        /** Prompt text sent to a Claude model. Use $ARGUMENTS for dynamic input. */
        prompt: string
        /** Model to use for the prompt. */
        model?: "opus" | "sonnet" | "haiku" | "opus[4m]" | "sonnet[4m]"
        /** Seconds before canceling. Default: 30 for prompt hooks. */
        timeout?: number
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Unique identifier for the completed task. */
        task_id: string
        /** Subject line of the completed task. */
        task_subject: string
        /** Full description of the completed task. */
        task_description: string
        /** Name of the teammate that completed the task. */
        teammate_name: string
        /** Name of the agent team. */
        team_name: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    agent: {
      settings: {
        /** Prompt text sent to the Claude model. $ARGUMENTS is replaced with hook context. */
        prompt: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Unique identifier for the completed task. */
        task_id: string
        /** Subject line of the completed task. */
        task_subject: string
        /** Full description of the completed task. */
        task_description: string
        /** Name of the teammate that completed the task. */
        teammate_name: string
        /** Name of the agent team. */
        team_name: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Unique identifier for the completed task. */
        task_id: string
        /** Subject line of the completed task. */
        task_subject: string
        /** Full description of the completed task. */
        task_description: string
        /** Name of the teammate that completed the task. */
        teammate_name: string
        /** Name of the agent team. */
        team_name: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  TaskCreated: {
    command: {
      settings: {
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Unique identifier for the created task. */
        task_id: string
        /** Subject line of the task. */
        task_subject: string
        /** Full description of the task. */
        task_description: string
        /** Name of the teammate assigned to the task. */
        teammate_name: string
        /** Name of the agent team. */
        team_name: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    prompt: {
      settings: {
        /** Prompt text sent to a Claude model. Use $ARGUMENTS for dynamic input. */
        prompt: string
        /** Model to use for the prompt. */
        model?: "opus" | "sonnet" | "haiku" | "opus[4m]" | "sonnet[4m]"
        /** Seconds before canceling. Default: 30 for prompt hooks. */
        timeout?: number
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Unique identifier for the created task. */
        task_id: string
        /** Subject line of the task. */
        task_subject: string
        /** Full description of the task. */
        task_description: string
        /** Name of the teammate assigned to the task. */
        teammate_name: string
        /** Name of the agent team. */
        team_name: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    agent: {
      settings: {
        /** Prompt text sent to the Claude model. $ARGUMENTS is replaced with hook context. */
        prompt: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Unique identifier for the created task. */
        task_id: string
        /** Subject line of the task. */
        task_subject: string
        /** Full description of the task. */
        task_description: string
        /** Name of the teammate assigned to the task. */
        teammate_name: string
        /** Name of the agent team. */
        team_name: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Unique identifier for the created task. */
        task_id: string
        /** Subject line of the task. */
        task_subject: string
        /** Full description of the task. */
        task_description: string
        /** Name of the teammate assigned to the task. */
        teammate_name: string
        /** Name of the agent team. */
        team_name: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  TeammateIdle: {
    command: {
      settings: {
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the teammate agent that became idle. */
        teammate_name: string
        /** Name of the agent team. */
        team_name: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name of the teammate agent that became idle. */
        teammate_name: string
        /** Name of the agent team. */
        team_name: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  UserPromptSubmit: {
    command: {
      settings: {
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** The user's submitted prompt text, before Claude processes it. */
        prompt: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Modified prompt text that replaces the original. Claude will see this instead of what the user typed. */
        prompt?: string
      }
    }
    prompt: {
      settings: {
        /** Prompt text sent to a Claude model. Use $ARGUMENTS for dynamic input. */
        prompt: string
        /** Model to use for the prompt. */
        model?: "opus" | "sonnet" | "haiku" | "opus[4m]" | "sonnet[4m]"
        /** Seconds before canceling. Default: 30 for prompt hooks. */
        timeout?: number
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** The user's submitted prompt text, before Claude processes it. */
        prompt: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Modified prompt text that replaces the original. Claude will see this instead of what the user typed. */
        prompt?: string
      }
    }
    agent: {
      settings: {
        /** Prompt text sent to the Claude model. $ARGUMENTS is replaced with hook context. */
        prompt: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** The user's submitted prompt text, before Claude processes it. */
        prompt: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Modified prompt text that replaces the original. Claude will see this instead of what the user typed. */
        prompt?: string
      }
    }
    http: {
      settings: {
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** The user's submitted prompt text, before Claude processes it. */
        prompt: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
        /** Modified prompt text that replaces the original. Claude will see this instead of what the user typed. */
        prompt?: string
      }
    }
  }
  WorktreeCreate: {
    command: {
      settings: {
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name for the worktree being created. */
        name: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Name for the worktree being created. */
        name: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
  WorktreeRemove: {
    command: {
      settings: {
        /** Shell command to execute. Receives JSON on stdin, returns JSON on stdout. */
        command: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Absolute path to the worktree being removed. */
        worktree_path: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
    http: {
      settings: {
        /** URL to POST the hook JSON payload to. */
        url: string
        /** Maximum execution time in milliseconds. Default: 5000ms. */
        timeout?: number
        /** When true, runs in background without blocking Claude Code. */
        async?: boolean
        /** When true, runs async but wakes model on exit code 2 (blocking error). */
        asyncRewake?: boolean
        /** Custom spinner message shown while the hook runs. */
        statusMessage?: string
        /** HTTP headers. Supports $VAR interpolation. Example: {"Authorization": "Bearer $TOKEN"}. */
        headers?: Record<string, unknown>
        /** Env var names allowed for $VAR interpolation in headers. */
        allowedEnvVars?: string[]
        /** Conditional execution using permission rule syntax (e.g. "Bash(git *)"). */
        if?: string
      }
      input: {
        /** Name of the hook event that fired. */
        hook_event_name:
          | "PreToolUse"
          | "PermissionRequest"
          | "PostToolUse"
          | "PostToolUseFailure"
          | "UserPromptSubmit"
          | "Notification"
          | "Stop"
          | "SubagentStart"
          | "SubagentStop"
          | "PreCompact"
          | "PostCompact"
          | "SessionStart"
          | "SessionEnd"
          | "Setup"
          | "TeammateIdle"
          | "TaskCreated"
          | "TaskCompleted"
          | "ConfigChange"
          | "WorktreeCreate"
          | "WorktreeRemove"
          | "InstructionsLoaded"
          | "Elicitation"
          | "ElicitationResult"
          | "StopFailure"
          | "CwdChanged"
          | "FileChanged"
        /** Unique identifier for the current Claude Code session. */
        session_id: string
        /** Absolute path to the conversation transcript JSON file. */
        transcript_path: string
        /** Current working directory of the Claude Code session. */
        cwd: string
        /** Active permission mode. */
        permission_mode:
          | "default"
          | "plan"
          | "acceptEdits"
          | "dontAsk"
          | "bypassPermissions"
        /** Subagent identifier. Present only inside a subagent context. */
        agent_id?: string
        /** Agent type name (e.g. "Bash", "Explore", "Plan"). Present inside subagent contexts. */
        agent_type?: string
        /** Absolute path to the worktree being removed. */
        worktree_path: string
      }
      output: {
        /** Set to false to stop Claude entirely. If omitted or true, Claude continues. */
        continue?: boolean
        /** Message displayed to the user when continue is false. */
        stopReason?: string
        /** When true, hides this hook's stdout from verbose mode output. */
        suppressOutput?: boolean
        /** Warning or info message shown to the user in the Claude Code UI. */
        systemMessage?: string
        /** Context string injected into Claude's conversation for the next response. */
        additionalContext?: string
      }
    }
  }
}
